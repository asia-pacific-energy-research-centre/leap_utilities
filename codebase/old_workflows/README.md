# Old Workflows

This folder holds workflow scripts that were moved out of the `codebase/` root
during cleanup on 2026-04-14.

They are kept for reference and recovery. The active workflow entrypoints remain
in `codebase/`; see `docs/workflow_inventory.md` for the current rationale.

